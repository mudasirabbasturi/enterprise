<?php

namespace App\Traits;

use Illuminate\Support\Facades\Auth;

trait HasPermission
{
    // public function hasPermission($route)
    // {
    //     $user = Auth::user();
    //     return $user && $user->hasPermission($route);
    // }
}

import type { AgTimeInterval, AgTimeIntervalUnit } from 'ag-charts-types';
import { DiscreteTimeScale } from './discreteTimeScale';
import type { NormalizedDomain, ScaleTickParams, ScaleTickResult } from './scale';
export declare class OrdinalTimeScale extends DiscreteTimeScale {
    readonly type = "ordinal-time";
    readonly defaultTickCount = 5;
    static is(value: unknown): value is OrdinalTimeScale;
    private _domain;
    private isReversed;
    set domain(domain: Date[]);
    get domain(): Date[];
    private _bands;
    get bands(): Date[];
    normalizeDomains(...domains: Date[][]): NormalizedDomain<Date>;
    ticks({ interval, maxTickCount, tickCount, }: ScaleTickParams<AgTimeInterval | AgTimeIntervalUnit | number>, domain?: Date[], visibleRange?: [number, number], extend?: boolean): ScaleTickResult<Date> | undefined;
    findIndex(value: Date): number | undefined;
}
